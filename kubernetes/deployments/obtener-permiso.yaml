apiVersion: apps/v1
kind: Deployment
metadata:
  name: obtener-permiso-app
  namespace: desarrollo-tt
  labels:
    app: obtener-permiso-app
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: obtener-permiso-app
  template:
    metadata:
      labels:
        app: obtener-permiso-app
    spec:
      containers:
      - name: obtener-permiso-app
        image: 123456789.dkr.ecr.us-east-1.amazonaws.com/desarrollo-tt/obtener-permiso-app:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8002
          name: http
        env:
        # NEXT_PUBLIC_API_BASE_URL es opcional - por defecto usa window.location.origin
        # - name: NEXT_PUBLIC_API_BASE_URL
        #   valueFrom:
        #     configMapKeyRef:
        #       name: app-config
        #       key: API_BASE_URL  # Debe estar en ConfigMap si se usa
        - name: ENVIRONMENT
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: ENVIRONMENT
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /
            port: 8002
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 8002
          initialDelaySeconds: 10
          periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: obtener-permiso-app
  namespace: desarrollo-tt
spec:
  type: ClusterIP
  selector:
    app: obtener-permiso-app
  ports:
  - port: 80
    targetPort: 8002
    protocol: TCP
    name: http
