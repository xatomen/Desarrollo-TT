apiVersion: apps/v1
kind: Deployment
metadata:
  name: obtener-permiso-app
  namespace: desarrollo-tt
  labels:
    app: obtener-permiso-app
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: obtener-permiso-app
  template:
    metadata:
      labels:
        app: obtener-permiso-app
    spec:
      imagePullSecrets:
      - name: ecr-secret
      containers:
      - name: obtener-permiso-app
        image: 892797891584.dkr.ecr.us-east-1.amazonaws.com/desarrollo-tt/obtener-permiso-app:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8002
          name: http
        env:
        - name: NEXT_PUBLIC_API_BASE_URL
          value: "http://api.jorgegallardo.studio"
        - name: ENVIRONMENT
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: ENVIRONMENT
        resources:
          requests:
            memory: "768Mi"
            cpu: "75m"
          limits:
            memory: "768Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /
            port: 8002
          initialDelaySeconds: 90
          timeoutSeconds: 5
          periodSeconds: 15
          failureThreshold: 8
        readinessProbe:
          httpGet:
            path: /
            port: 8002
          initialDelaySeconds: 45
          timeoutSeconds: 5
          periodSeconds: 10
          failureThreshold: 8

---
apiVersion: v1
kind: Service
metadata:
  name: obtener-permiso-app
  namespace: desarrollo-tt
spec:
  type: ClusterIP
  selector:
    app: obtener-permiso-app
  ports:
  - port: 80
    targetPort: 8002
    protocol: TCP
    name: http
